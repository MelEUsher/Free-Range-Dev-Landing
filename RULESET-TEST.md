# Ruleset Test

Throwaway markdown file. It exists to prove that a markdown-only pull request still merges under
the `main protection` ruleset, which is the exact case that would break article publishing if the
required status checks were chosen wrong.

Delete this file and its branch once the test passes.
